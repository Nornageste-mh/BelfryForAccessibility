# 更新日志

## 版本方案

| 形式 | 含义 |
|---|---|
| `0.1.x.y` | **现行正式序列。** 标注（annotated）标签，形如 `v0.1.0.1` |
| `v0.1.x.y` | 标签名带 `v`，代码里的 `<Version>` 不带 |

**版本号只用纯数字四段，不加字母。** 这条是硬约束，不是洁癖：
`BepInPlugin` 的版本参数是 `System.Version`，而 `System.Version` 解析
`0.1.0a` 会**直接抛异常** —— 后果不是显示成怪样子，是**插件被整个跳过**，
日志里只留一行 `version is invalid`，游戏看起来和没装补丁一模一样。

> 上一个仓库（TransparentHer）在这里踩过：发布名一度是 `v0.5.8a`
> （`a` = 在 v0.5.8 基础上的一次修订），但代码里只能写 `0.5.8.1`，
> 同一个版本两套写法。本仓库从一开始就只走数字四段。
>
> 想表达「第五版修订」用第四位：`0.1.0.1`。

`mod/build.ps1` 里有一道守卫：编译前先解析 `BepInPlugin` 的版本号，
不合法就直接中止，并核对它与 `csproj` 的 `<Version>` 是否一致。

---

## 版本清单

| 版本 | 提交 | 主要内容 |
|---|---|---|
| `0.1.0.0` | `ca691cf` | **首个版本。** 朗读无配音剧情 / 手书演出 / 选项 / 手机消息，数字键选择，菜单键盘导航，标题旋转菜单改造 |
| **`0.1.0.1`** | `1e69d08` | **修复「重读」在快进与选项上的三个漏洞**（实机反馈）：有配音的行、选项播报、快进经过的行都没进重读缓冲区 |

---

## 0.1.0.1 — 修复「重读」在快进与选项上的三个漏洞

来自实机反馈：**按住 Ctrl 快进，遇到选项按退格念不出选项；松开 Ctrl 停下来，
按退格也念不出停在的这一句。**

根因是「重读缓冲区」的覆盖面不全。上一作把所有朗读都收进唯一出口
`Say()`，念过什么和能重念什么是同一份名单；这一作第一版漏掉了三条路径。
现在补上。

### 1. 有配音的行没有进缓冲区（静默 bug，反馈里没提到）

`Consume()` 遇到有配音的行会 `Speech.Stop()` 后直接返回，**没有把这一行记进
缓冲区**。于是按退格念的是**上一句**，玩家会以为「这一句翻不回来」。
上一作 v0.5.x 修过一模一样的 bug，这一版又犯了一次。

现在有配音的行同样进缓冲区 —— 补丁只是不自动朗读它，玩家主动按退格
仍然应该听得到文字。

### 2. 选项播报绕过了缓冲区

`Choices.AnnounceChoices` 直接调 `Speech.Speak`，没经过 `Reader`，
所以停在选项上按退格什么也念不出来。

现在：

- 选项播报走 `Reader.Say`，进缓冲区
- 另外单独存一份播报文本，`Reader.Repeat()` 发现当前有选项时**优先**重念选项
  （这时屏幕上没有「一句台词」可读）
- 即使把配置 `朗读选项` 关掉，播报文本照样会存 ——
  关掉的只是自动播报，退格是玩家的主动请求，仍然要响应

### 3. 快进经过的行没有进缓冲区

快进（按住 Ctrl / `F`）走的是 `ExecuteDialogue` 的 `isInstant` 分支，
它**直接写 `dialogueText.text`，不调用 `StartTyping`**，
所以 `Consume()` 一次都没跑过，缓冲区停在快进之前。

现在 `Reader.NoteScene(scene, isInstant)` 会把这类行**只记不念**地更新进缓冲区
（用 `HarmonyArgument(2)` 按位置取参数，不依赖运行时参数名）。
另加配置 `快进停止时补念当前句`（默认开）：检测 `ScriptEngine.isFastForward`
的下降沿，松开 Ctrl 的那一刻把停在的句子念出来。
停在选项上时不重复念 —— 选项出现的那一刻已经完整播报过一遍了。

### 顺带修掉的两个相关问题

- **选项列表的生命周期**。原来只在「按数字键选中」时清空，玩家**用鼠标点**
  选项根本不经过这段代码，列表会一直留着 —— 之后每次按退格都会去念那几个
  早就消失的选项，而念不到当前台词。现在改成：一句新台词开始显示时
  （`Reader.Consume`）作废剧情选项，一条新手机消息到达时作废手机选项。
- **剧情选项与手机选项分开存播报文本**。手机聊天的后续消息不走
  `StartTyping`，不会顺手清掉剧情选项，两者生命周期独立。

### 顺带加的一道保险

`Reader.Say()` 的 0.4 秒同句去重**不作用于重读路径** ——
`Repeat()` 与 `Choices.RepeatAnnounce()` 直接调 `Speech.Speak`。
否则「刚念完马上按退格再听一遍」这个正当操作会被去重吃掉。

### 复测要点（**尚未实机复测**）

下面五条是这次修复要验证的行为，**目前只做了代码层面的推理，没有实机确认**：

| 操作 | 期望 |
|---|---|
| 按住 `Ctrl` 快进 → 遇选项 → `Backspace` | 完整念一遍选项 |
| 按住 `Ctrl` 快进 → 松开 | 自动念出停在的这一句 |
| 松开后 `Backspace` | 再念一遍 |
| 在**有配音**的台词上 `Backspace` | 念这一句的文字（以前念成上一句） |
| **鼠标**点掉选项 → `Backspace` | 念当前台词 |

复测通过后会补一份实机记录，并与 0.1.0.0 的验证证据一并放进 `probe/evidence/`。

---

## 0.1.0.0 — 首个版本

面向 Steam 正式版《钟塔》（The Belfry，AppID 2373260）。

### 朗读

- **朗读无配音剧情文本**。挂 `UISceneController.StartTyping`。
  判定方式是 `DialogueCommandExecutor.ExecuteDialogue` 收到的
  `DialogueScene.VoiceFilename` 是否为空 —— 全作 3812 行里
  1491 行有配音（39.1%），其余 2321 行靠这个功能才听得到。
  有配音的行不朗读，只放语音，并打断上一句读屏。
- **朗读手书演出**。第 5 章那 44 行走的是
  `ShoushuDialoguePresenter.PresentLineAsync`，和普通台词不是同一条通道，
  所以单独挂了一个补丁点。
- **朗读时带说话人**。剧本里存的名字是 `【 韩冬 】`（全角括号 + 两侧空格），
  照读会变成「左黑括号 空格 韩冬 空格 右黑括号」，所以做了剥离；
  `【 ?  ? 】` 这种遮蔽名统一读作「？？？」；
  `【 许堇/韩冬 】` 读作「许堇、韩冬」。
- **重读按键**（默认 `Backspace`）。

### 选项

- **朗读选项**，一次念完并编号。
- **数字键 1-9 选择**。不是直接调跳转回调，而是保存按钮对象、
  选中时 `button.onClick.Invoke()` —— 因为 `ChoiceHandler.OnChoiceButtonClicked`
  还要销毁散在画面上的选项按钮、恢复主 UI、记录选择，
  漏掉这些会让按钮留在屏幕上。
- 本作剧情选项只有 3 处（表 4 两处、表 5 一处）。
- **限时选择**：`AutoDestroyAfterTime` 的 `ref float delay` 会被拉长到
  配置值（默认 20 秒）。本作的剧本表里目前没有 `TypeName == "实时"` 的行，
  这一项是为游戏更新留的保险。**倒计时只能拉长不能取消** ——
  到点会走这一批最后一条的 `ToNumber`，也就是「什么都不做」的沉默分支。

### 手机

- 朗读手机聊天消息（`PhoneDialogueManager.AddMessage`）。
- 朗读表情贴纸提示（`HandleExpressionMessage`）。
- 手机里的选项也能用数字键选（`HandleChoiceMessages`）。

### 界面导航

- **`Tab` 进入 / 退出导航模式**，上下选、左右调滑条、回车空格激活、
  Home/End 跳首尾、PageUp/PageDown 换面板组。
- 标题画面的旋转环形菜单在导航模式下换成位置固定的线性列表，
  并给五个纯图片按钮（开始游戏 / 读取存档 / 画廊 / 系统设置 / 退出游戏）
  做了中文别名 —— 它们的文字画在图片上，TMP 里一个字都没有。
- **退出前二次确认**：标题的「退出游戏」按一下就会关掉整个游戏，
  而且它和「开始游戏」在同一个圆环上，读屏用户区分不了，所以补了一道确认。
- 默认把社交链接栏（`Attention`：Steam / 小黑盒 / Bilibili）从导航里排除。
- 导航模式下拦住游戏自己的「推进剧情」（`GameFlowManager.HandleAdvanceInput`），
  避免一次空格既激活控件又推进剧情。

### 与上一作不同的三处技术决定

1. **没有**常驻关掉 `EventSystem.sendNavigationEvents`。
   《钟塔》自己就在用 EventSystem 选中态（横条导航、面板提交、
   `GlobalFocusFixer` 焦点修正、存档槽 `ISubmitHandler`），
   常驻关掉会打断这些原生功能。改成只在导航模式确实要接管那一下按键时，
   挂 `StandaloneInputModule.SendSubmitEventToSelectedObject` 掐掉 uGUI 的提交。
2. **`KeyEdge` 按下闩锁**。开发期用脚本驱动游戏时观测到合成输入下
   `Input.GetKeyDown` 会在一次按下里报两次，导致导航模式「进入→退出→进入」。
   现在所有一次性动作键（Tab / 方向键 / 回车 / 空格 / 数字键 / 重读键）
   都要求「一次按下-抬起才算一次」。
3. **补丁点全部按运行时实查的方法名挂**，不靠猜。

### 已知限制

- 不覆盖：CG / Spine 立绘的口述影像、视频口述影像、成就弹出提示、
  历史回顾面板的逐条朗读。
- 未做配音台词的 TTS 预生成。
- 中文朗读质量取决于所选后端与系统语音包。
