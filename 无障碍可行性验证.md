# 《钟塔 Belfry》无障碍可行性验证

> 结论先行：**可行，且成本比前两作都低**。
> 游戏是 Unity 2022.3.43f1c1 + **Mono** 后端，与《透明的她与真实的我》
> 同引擎版本、同开发商（**可味玩KawayiPlay**），BepInEx 5 那条流水线可以直接复用。
> 全部关键补丁点都已在实机确认存在。

本文所有结论都标了出处：`反编译`指 `decompiled/` 里的 Assembly-CSharp 反编译源码，
`exe`指对游戏资源 / 运行时的实测。凡是没有实测支撑的推断都明确写成「待验证」。

---

## 1. 目标游戏

| 项目 | 值 | 出处 |
| --- | --- | --- |
| 中文名 | 钟塔 | Steam 商店 |
| 英文名 | The Belfry | `The Belfry_Data/app.info` |
| 开发商 | 可味玩KawayiPlay（`app.info` 里写的是 `Kawayi Play`） | `The Belfry_Data/app.info` 第一行 |
| Steam AppID | 2373260 | `steamapps/appmanifest_2373260.acf` |
| VNDB | v61157 | 公开资料（**本仓库未直接核实**） |
| 发售 | 2026 年 4 月 1 日 | 公开资料（**本仓库未直接核实**） |
| 可执行文件 | `The Belfry.exe` | exe |
| 引擎 | Unity 2022.3.43f1c1 | `globalgamemanagers` 头部版本串 |
| 脚本后端 | **Mono**（`Managed/` 下 127 个托管程序集） | exe |
| 主要第三方库 | Addressables、Spine、DOTween、TextMeshPro、Steamworks.NET、Newtonsoft.Json、Cinemachine | `Managed/` |
| 剧本规模 | 3812 行，1491 行有配音（39.1%） | `extracted/` 五张表统计 |
| 安装体积 | 2922 MB | exe |

本文的原则是**标清出处**：`app.info`、`appmanifest`、`globalgamemanagers`、
`Managed/`、`extracted/` 都是本机实查；标了「公开资料」的几项来自 VNDB 与商店页，
本项目的开发环境访问不到它们（Steam 商店页被网络策略拦截，SteamDB 明确拒绝
第三方抓取），所以只转述、不当成自己核实过的事实。

### 顺带纠正一条公开资料的错误

公开资料常把本作描述成「全 Live2D」。**不对** —— 游戏程序集里带的是
**Spine**：`Managed/` 下有 `spine-csharp.dll` 与 `spine-unity.dll`，
反编译代码里角色用的是 `SkeletonAnimation` / `SkeletonGraphic`，
`SpineVisualsManager` 里有完整的骨骼、皮肤、动画轨道管理。

这不影响补丁（两条路都不产出可朗读的文本），但记下来，
免得后来者照着「Live2D」去找切入点。

「Mono」这一条是整个可行性判断的分水岭：它意味着可以直接用 BepInEx 5 +
HarmonyX 打托管补丁，《The NOexistenceN of you AND me》那种
IL2CPP + Il2CppInterop + 上游 PR 打补丁的复杂度**完全不存在**。

### 「同一个开发商」是怎么确认的

不是靠商店页面，是拿游戏文件本身对的 —— 三份 `app.info` 的开发商字段：

| 游戏 | `app.info` 里的开发商 | 结论 |
| --- | --- | --- |
| 钟塔 Belfry | `Kawayi Play` | ← 本作 |
| 透明的她与真实的我 | `Kawayi Play` | 同一个 `Kawayi Play` |
| The NOexistenceN of you AND me | `Nino` | **另一个开发商** |

`app.info` 在 `<游戏名>_Data/` 下，是纯文本两行（开发商 / 产品名），
存档目录名也取自这里（`%USERPROFILE%\AppData\LocalLow\Kawayi Play\`）。
所以「《钟塔》和《透明的她》是一家」这句话是有据可查的，
而《The NOexistenceN》虽然题材相近，却是另一家做的 ——
这也解释了为什么它的技术栈完全不同（Naninovel + IL2CPP）。

---

## 2. 剧情文本的存放位置

游戏把剧本放在 `resources.assets` 里，是 5 个 **TextAsset**，名字规律：

| 资源名 | 大小 | 行数 |
| --- | --- | --- |
| `TheBelfrySchedule1` | 535 050 B | 684 |
| `TheBelfrySchedule2` | 764 482 B | 962 |
| `TheBelfrySchedule3` | 488 978 B | 627 |
| `TheBelfrySchedule4` | 438 849 B | 576 |
| `TheBelfrySchedule5` | 751 240 B | 963 |
| **合计** | **2 978 599 B** | **3812** |

内容格式是 JSON 数组，每个元素是一行剧本，字段与
`DialogueScene`（`decompiled/DialogueScene.cs`）一一对应：

```json
{
  "Number": 1,
  "VoiceFilename": "",
  "TypeName": "转场",
  "CharacterName": "",
  "Dialogue": "",
  "ChoiceText": "",
  "ToNumber": "",
  "Background": "xiaojin_wish_1",
  ...
}
```

> 注意：运行时真正喂给 `ScriptEngine` 的是 Inspector 上挂的
> `DialogueSceneData` ScriptableObject（`ScriptEngine.LoadCurrentDialogueSceneData`
> 直接读 `dialogueSceneDatas[0].dialogueScenes`），
> 这 5 个 TextAsset 是它们的导入源。两者内容一致，
> 但**补丁不要去解析 TextAsset** —— 直接读运行时对象更稳，也不碰版权内容。

提取方式见 `tools/dump_textassets.py`（依赖 UnityPy）。

---

## 3. 剧本结构统计

对 5 张表全量统计（`tools/script_stats.py`，产物 `extracted/script_stats.txt`）：

### 3.1 行类型

| TypeName | 行数 | 含义 |
| --- | --- | --- |
| 对话 | 2554 | 有说话人的台词 |
| 旁白 | 982 | 无说话人的叙述 |
| 转场 | 197 | 背景 / BGM / 运镜切换 |
| 手书 | 44 | 第 5 章的手绘演出（另一套显示通道） |
| 成就 | 16 | 解锁 Steam 成就 |
| 选择 | 4 | 选项行 |
| 动画 | 4 | 视频播放 |
| 转表 | 4 | 切换到下一张表 |
| 电话开 / 电话关 | 各 3 | 手机聊天 |
| 结局 | 1 | 结局 |

### 3.2 配音覆盖

**1491 / 3812 = 39.1%** 的行有 `VoiceFilename`（唯一值 1480 个，
命名形如 `voice_1_4_xiaojin`、`voice_1_20_xvjin`）。

这条数字直接决定补丁的核心策略：**只有 39% 的行有语音兜底，
剩下 61%（2321 行）对视障玩家是完全静音的**。补丁必须朗读这 2321 行，
同时避开有语音的 1491 行，否则读屏会和配音叠在一起。

### 3.3 分支

把 `ToNumber` 里**真正能解析成跳转**的行挑出来（`Tools` 见
`extracted/branches.txt`）——注意 `ToNumber` 在 JSON 里既有字符串也有
浮点数，`"0.0"` 有 1532 行，`int.TryParse("0.0")` 会失败，所以它们不是跳转：

| 表 | 行号 | 类型 | ToNumber | 内容 |
| --- | --- | --- | --- | --- |
| 4 | 564 | 选择 | 566 | 找寻踪影 |
| 4 | 565 | 选择 | 571 | 跨过门扉 |
| 5 | 755 | 选择 | 756 | 前往「彼岸」 |
| 5 | 869 | 选择 | 870 | 回归「此岸」 |
| 4 | 570 / 574 | 旁白 | 575 | 两条分支汇合 |
| 1 / 2 / 3 / 4 | 684 / 962 / 627 / 576 | 转表 | i,1 | 串起五张表 |

**全作只有 3 处选项。**《钟塔》是一部近乎线性的作品 ——
这与《透明的她与真实的我》有 246 处限时选择形成鲜明对比。

顺带一个重要结论：**本作没有 `TypeName == "实时"` 的行**，
也就是没有限时选择。（`ChoiceHandler.ExecuteRealTimeLogic` /
`AutoDestroyAfterTime` 的代码路径仍在，是为将来更新留的，补丁一并保留。）

### 3.4 说话人

| 原名（JSON 里就这么存） | 行数 |
| --- | --- |
| `""`（旁白，无名） | 1256 |
| `【 韩冬 】` | 1065 |
| `【 许堇 】` | 937 |
| `【 小堇 】` | 481 |
| `【 老妈 】` | 26 |
| `【 ?  ? 】` | 23 |
| `【 老爸 】` | 8 |
| `【 老阿姨 】` | 8 |
| `【 护士 】` | 4 |
| `【 许堇/韩冬 】` / `【 韩冬/小堇 】` / `【 小堇/许堇/韩冬 】` | 各 1 |

名字带**全角方括号和两侧空格**，照读是「左黑括号 空格 韩冬 空格 右黑括号」，
必须剥掉；`【 ?  ? 】` 是故意的遮蔽名，统一读作「？？？」。

---

## 4. 运行时架构与补丁点

驱动链（全部来自反编译实查）：

```
ScriptEngine.DisplayNextLine()
  └─ ExecuteCurrentCommand(scene)
       ├─ DialogueCommandExecutor.ExecuteDialogue(scene, index, isInstant)
       │    ├─ UISceneController.UpdateCharacterUI(scene)        设说话人
       │    ├─ UISceneController.AddToHistory(scene, index)
       │    └─ UISceneController.StartTyping(localizedDialogue)  ← 显示台词
       │         （手书行走 ShoushuDialoguePresenter.PresentLineAsync）
       ├─ 选择 → DialogueCommandExecutor.ExecuteSelection
       │         └─ ChoiceHandler.ExecuteSelectionLogic(scene, list, jumpCallback)
       ├─ 实时 → ChoiceHandler.ExecuteRealTimeLogic(...)
       └─ 电话 → DialogueCommandExecutor.ExecutePhoneDialogue
```

补丁点清单（每一项都在实机 `Harmony PatchAll` 里验证过存在）：

| 功能 | 挂载点 | 可访问性 |
| --- | --- | --- |
| 抓住当前行 + 配音判定 | `DialogueCommandExecutor.ExecuteDialogue` | public，参数含 `DialogueScene` |
| 朗读无配音台词 | `UISceneController.StartTyping(string)` | public |
| 朗读手书（44 行） | `ShoushuDialoguePresenter.PresentLineAsync(string,bool)` | public |
| 朗读选项 / 数字键选择 | `ChoiceHandler.ExecuteSelectionLogic` | public，参数含选项 `List<DialogueScene>` |
| 限时选择延长 | `ChoiceHandler.AutoDestroyAfterTime(float,…)` | private，**迭代器** |
| 手机消息 | `PhoneDialogueManager.AddMessage(string,bool,…)` | private |
| 手机选项 | `PhoneDialogueManager.HandleChoiceMessages()` | private |
| 手机表情 | `PhoneDialogueManager.HandleExpressionMessage(PhoneDialogue)` | private |
| 拦住剧情推进 | `GameFlowManager.HandleAdvanceInput()` | private |
| 拦住面板重复提交 | `MainUIController.ExecuteClickOnSelectedElement()` | private |
| 标题菜单让位 | `ClockwiseMenuController.HandleKeyboardGamepadInput()` | private |
| uGUI submit 通路 | `StandaloneInputModule.SendSubmitEventToSelectedEvent` | protected virtual |

> `AutoDestroyAfterTime` 是**迭代器**：对它用「Prefix 返回 false 跳过」
> 会让它返回 `null`，而调用方是 `StartCoroutine(...)`，直接 NRE。
> 只能改 `ref float delay`。这条坑在上一作 v0.5.x 已经踩过一次。

### 4.1 这一作特有的三处麻烦

**(1) 标题菜单是旋转环形菜单，不是平铺按钮。**

`ClockwiseMenuController` 把 Gallery / Load / Start / Options / Exit 五个
图标排在一个圆环上，上下方向键是**转动**圆环，回车/空格无条件执行
**正中间**那一项（`ExecuteButtonAction(_buttonDataList[2])`），
跟画面上高亮哪个无关。转动靠的是把 `_buttonDataList` 重新排序。

对读屏用户来说这是最不友好的形态：位置一直在变，而且按键与目标不对应。

处理办法：导航模式下把 `HandleKeyboardGamepadInput` 让开，
由 `UiNav` 提供一个位置固定的线性列表；激活时走按钮自己的
`onClick` → `RotateToCenter`，游戏会先转到中心再执行，
既保留了动画，也不改变游戏行为。五个按钮的文字**画在图片上**
（运行时转储里 `Gallery [Image,Button,CanvasGroup]`，没有任何 TMP 文本），
所以需要按对象名做中文别名。

**(2) 游戏自己就在用 EventSystem 选中态。**

《透明的她与真实的我》全代码没有一处 `SetSelectedGameObject`，
所以那一作是**常驻**关掉 `EventSystem.sendNavigationEvents` 一劳永逸。
《钟塔》不能这么干：

- `MainBarNavigationController` 用 `SetSelectedGameObject` 做横条导航
- `MainUIController.ExecuteClickOnSelectedElement` 面板内「回车=点当前项」
- `GlobalFocusFixer` 每 0.2 秒检查并修正焦点
- `SaveSlotUI` 实现了 `ISubmitHandler`

改成**只在导航模式确实要接管这一下按键时**才掐掉 uGUI 的 submit
（挂 `StandaloneInputModule.SendSubmitEventToSelectedObject`），
非导航模式下游戏行为原样保留。

**(3) 游戏原生占用大量按键。**

`InputManager.InitDefaultBindings` 里已经绑了：

```
A 自动      F 快进      S 快速存档   D 快退      Q 下一选项
W 流程图    P 返回      N 下一场景   B 返回选项   V 收藏语音
R 历史      L 读档      Ctrl 跳过    Del 开关UI   F1-F10 各项功能
空格 / 回车 / 小键盘回车 推进          Esc / 右键 返回
```

所以补丁自用键只能选 **1-9（选项）、0（沉默）、退格（重读）、Tab（导航）**
和方向键 / Home / End / PageUp / PageDown。

---

## 5. 语气与可覆盖范围

| 内容 | 能否让读屏用户拿到 | 方式 |
| --- | --- | --- |
| 无配音台词 / 旁白（2321 行） | ✅ | 朗读，带说话人 |
| 有配音台词（1491 行） | ✅ | 只放语音，读屏让位 |
| 手书演出（44 行） | ✅ | 朗读（`PresentLineAsync`） |
| 选项（3 处 + 手机选项） | ✅ | 一次念完 + 数字键选择 |
| 手机聊天消息 / 表情 | ✅ | 朗读 |
| 标题 / 存读档 / 设置 / 画廊 | ✅ | Tab 导航 + 朗读 |
| 成就解锁 | ⚠️ 未覆盖 | 游戏不弹提示，需要额外挂 `TriggerSteamAchievement` |
| CG / 立绘画面 | ❌ 不做口述影像 | 本项目范围外 |
| 视频（4 处动画） | ❌ 不做口述影像 | 本项目范围外 |

---

## 6. 复现方式

```powershell
# 1. 提取剧本（需要 UnityPy）
python tools/dump_textassets.py
python tools/script_stats.py

# 2. 反编译游戏逻辑（需要 ilspycmd）
ilspycmd -p -o decompiled "<游戏目录>\The Belfry_Data\Managed\Assembly-CSharp.dll"

# 3. 实机探针：把 BepInEx 挂进游戏，转储运行时场景树
cd probe
.\run_probe.ps1
```

`extracted/` 与 `decompiled/` 均为游戏版权内容，**已被 `.gitignore` 排除，
请勿提交或分发**。
