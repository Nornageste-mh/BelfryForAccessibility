# 《钟塔》无障碍补丁 · 实机验收问卷

> 这份问卷是给**实机测试**用的。补丁的每个补丁点都已经在开发机上确认
> 「方法存在、能被 Harmony 挂上、能被调用」，但**功能是否真的对玩家可用**
> 只能靠人戴耳机实测。
>
> 请按顺序做，每条只需勾「通过 / 不通过」，不通过的把现象和
> `BepInEx\LogOutput.log` 里对应的片段贴回来。
>
> 建议先把配置里的 **`界面诊断日志` 改成 `true`** 再开始 ——
> 这样日志里会逐条记下「本该念出去的东西」，出问题时能直接看出是
> **没触发朗读** 还是 **触发了但读屏没出声**。

> ### 关于剧透
>
> **本问卷刻意不写任何台词原文、选项文字、角色名与章节位置。**
> 「期望听到什么」一律用结构描述（「旁白被念出来」「带说话人前缀」），
> 不引用具体内容 —— 填问卷的人自己就在玩游戏，不需要我们替他剧透；
> 而这份文件是公开的。
>
> 所以你会看到「流程后段」「有配音的角色」这类写法。
> 需要核对具体文本时，读 `BepInEx\LogOutput.log` 里的 `[朗读]` 行即可。

---

## 0. 环境记录（开始前填一次）

| 项 | 值 |
| --- | --- |
| 游戏版本（Steam 属性里看） | |
| 补丁版本 | |
| 读屏软件与版本（NVDA / 争渡 / 其他） | |
| 语音后端（看日志 `语音后端就绪: ___`） | |
| Windows 版本 | |
| 是否装了中文语音包 | |

---

## 1. 加载与启动

| # | 检查项 | 期望 | 结果 |
| --- | --- | --- | --- |
| 1.1 | 从 Steam 正常启动游戏 | 游戏能正常进到标题画面，不崩 | ☐ |
| 1.2 | 启动约 6 秒后 | 听到「钟塔无障碍补丁已加载。按 Tab 进入菜单导航，退格键重读当前这一句。」 | ☐ |
| 1.3 | 打开 `BepInEx\LogOutput.log` | 有 `Loading [The Belfry A11y Reader 0.1.0.1]`、`语音后端就绪: ___`、`Harmony 补丁已应用。` | ☐ |
| 1.4 | 日志里搜 `Failed to patch` / `Harmony 补丁失败` | **一条都没有** | ☐ |

> 1.2 听不到但 1.3 正常 → 是读屏/语音后端的问题，不是补丁没加载。
> 1.3 里没有 `Loading` 那行 → BepInEx 没挂上，检查 `winhttp.dll` 与 `BepInEx\core\`。

---

## 2. 标题菜单导航（本作最特殊的一处）

标题画面原本是一个**旋转环形菜单**：上下键在「转」它，
回车/空格无条件执行**正中间**那一项，跟你看到的高亮无关。
补丁在导航模式下把它改成位置固定的线性列表。

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 2.1 | 在标题按 `Tab` | 听到「导航模式，第 1 组，共 5 项。开始游戏，按钮。1 / 5」 | ☐ |
| 2.2 | 按 `↓` 四次 | 依次听到：读取存档 2/5、画廊 3/5、系统设置 4/5、退出游戏 5/5 | ☐ |
| 2.3 | 再按 `↓` 一次 | 绕回「开始游戏 1/5」 | ☐ |
| 2.4 | 按 `Home` | 跳回「开始游戏 1/5」 | ☐ |
| 2.5 | 按 `End` | 跳到「退出游戏 5/5」 | ☐ |
| 2.6 | 列表里**不应该**出现 | `Attention` / `Steam` / `Bilibili` / `Xiaoheihe` | ☐ |
| 2.7 | 在「退出游戏」上按一次 `空格` | 听到二次确认「这是退出游戏。再按一次回车或空格确认退出，按别的键取消。」 | ☐ |
| 2.8 | 接着按 `↓` | 取消，**游戏不退出** | ☐ |
| 2.9 | 回到「开始游戏」，按 `空格` | 听到「已激活 开始游戏」，随后进入剧情 | ☐ |

> 2.6 不通过（社交链接混进来了）→ 检查配置 `排除的对象名` 是不是 `Attention`。
> 2.8 如果游戏退出了 → **严重问题**，请立刻反馈。

---

## 3. 剧情朗读（核心功能）

全作 3812 行，其中 1491 行有配音（39.1%）。
**设计是：有配音的不念，只放语音；没配音的必须念。**

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 3.1 | 进入剧情后听开场 | **旁白**被逐句念出来（旁白 = 没有说话人的叙述句，不带前缀） | ☐ |
| 3.2 | 推进到有说话人的台词 | 被念出来，且**带说话人前缀**，形如「某某：『……』」 | ☐ |
| 3.3 | 推进到**有配音**的台词 | **不朗读**，只听到游戏语音 | ☐ |
| 3.4 | 按一次 `空格` | 剧情**只推进一行**（不会跳两行） | ☐ |
| 3.5 | 在一句话正在打字时按 `空格` | 先把当前句补全，再按一次才推进 | ☐ |
| 3.6 | 按 `Backspace` | 重读当前这一句 | ☐ |
| 3.7 | 用游戏原生的 `A`（自动）跑一段 | 自动播放期间朗读节奏跟得上，不会错行 | ☐ |
| 3.8 | 用游戏原生的 `F`（快进）跑一段 | 快进时**不刷屏朗读**；停下来后能正常朗读当前行 | ☐ |
| 3.9 | 用游戏原生的 `R`（历史回顾）翻页 | 不崩、不卡（本项不要求朗读历史） | ☐ |

> 3.3 不通过（有配音的台词也被念了）→ 说明配音判定失效，
> 请把该行的 `[朗读]` 日志贴回来。
> 3.4 不通过（一次跳两行）→ 按键归属有问题，**严重**。

---

## 4. 选项与「重读」（**重点复测项**）

本作剧情选项只有 **3 处**，都在流程后段。**选项文字这里不写**（那是剧情）。

`0.1.0.1` 修的就是这一块：重读缓冲区曾经漏掉三条件路径。

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 4.1 | 走到第一个选项处 | 一次听完：「出现 N 个选项。第 1 项：……。第 2 项：……。按数字键选择。」 | ☐ |
| 4.2 | 按 `1` | 进入第 1 项对应的分支，剧情继续 | ☐ |
| 4.3 | 读档回到选项前，按 `2` | 进入第 2 项对应的分支 | ☐ |
| 4.4 | 选项选中后 | 画面上的选项按钮消失，剧情继续 | ☐ |
| 4.5 | 在选项出现时按 `9`（超出项数） | 听到「没有第 9 项。」，**不死机** | ☐ |
| 4.6 | **按住 `Ctrl` 快进到选项处，松开，按 `Backspace`** | **把这一批选项完整重念一遍** | ☐ |
| 4.7 | **按住 `Ctrl` 快进一段，松开** | **自动把停在的这一句念出来** | ☐ |
| 4.8 | 接 4.7，再按 `Backspace` | 再念一遍停在这一句 | ☐ |
| 4.9 | **在「有配音」的台词上按 `Backspace`** | 念出**这一句**的文字（`0.1.0.0` 会错念成上一句） | ☐ |
| 4.10 | **用鼠标点掉一个选项，之后按 `Backspace`** | 念当前台词（不会去念早已消失的选项） | ☐ |

> 4.1 完全没播报、且日志里也没有 `[Choices]` 行 → 选项挂钩没生效。
> 播报了但数字键无反应 → 按键归属问题。
> 4.6–4.10 就是 `0.1.0.1` 修的五个点，**这五条目前还没有实机复测过**。

---

## 5. 手机 / 电话剧情

全作有 3 段电话/聊天剧情，分布在前中后期。

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 5.1 | 走到电话剧情 | 手机聊天消息被逐条念出来，带「对方：」「我：」前缀 | ☐ |
| 5.2 | 遇到表情贴纸 | 听到「对方发来一张表情图片。」 | ☐ |
| 5.3 | 如果有手机选项 | 被念出来，且能用数字键选 | ☐ |

---

## 6. 手书演出（流程后段）

流程后段有一段 44 行的手绘演出，走的是**和普通台词不同的显示通道**
（`ShoushuDialoguePresenter`），是单独挂的补丁点。

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 6.1 | 进入手书段落 | 每一行都被念出来（这 44 行**全都没有配音**） | ☐ |
| 6.2 | 手书里的淡入淡出 | 朗读不重叠、不抢话 | ☐ |

---

## 7. 存读档 / 设置 / 画廊

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 7.1 | 标题 → `Tab` → 选「读取存档」→ `空格` | 打开存档界面并朗读当前项（存档槽会念出章节与时间） | ☐ |
| 7.2 | `↓` 逐个走存档槽 | 每个槽位的名字/时间不同，能分辨 | ☐ |
| 7.3 | 在存档槽上按 `空格` | **只激活一次**（不会存/读两遍） | ☐ |
| 7.4 | 进「系统设置」 | 页签与每一行都能念出来（显示模式、文本显示速度等） | ☐ |
| 7.5 | 在滑条（文本显示速度 / 自动模式速度）上按 `←` `→` | 数值变化**并被朗读** | ☐ |
| 7.6 | 进「画廊」 | 页签（CG / 视频 / 音乐 / 语音）能念出来 | ☐ |
| 7.7 | 按 `PageUp` / `PageDown` | 能在不同的面板组之间切换 | ☐ |
| 7.8 | 再次按 `Tab` | 听到「已退出导航模式。」，鼠标操作恢复正常 | ☐ |

---

## 8. 回归：不装/关掉补丁时游戏本身要正常

| # | 操作 | 期望 | 结果 |
| --- | --- | --- | --- |
| 8.1 | 把 `菜单键盘导航` 改成 `false`，重启 | 游戏完全恢复原本的鼠标 + 原生键盘操作 | ☐ |
| 8.2 | 关掉补丁后（删 `winhttp.dll`） | 游戏与原版**行为一致**，无残留 | ☐ |

---

## 9. 稳定性

| # | 检查项 | 期望 | 结果 |
| --- | --- | --- | --- |
| 9.1 | 连续游玩 30 分钟以上 | 不崩溃、不卡顿 | ☐ |
| 9.2 | 反复存档 / 读档 10 次以上 | 不崩溃（历史上「定时重扫界面」曾在场景销毁瞬间触发原生崩溃，现在的实现只在下一帧重扫） | ☐ |
| 9.3 | 剧情中途按 `Esc` 开菜单再关掉 | 朗读与按键归属都正常 | ☐ |
| 9.4 | 走到任意一个结局 | 结局文本被念出来，不崩 | ☐ |

---

## 10. 反馈时请附上

1. 本问卷的勾选结果
2. 出问题那一段的 `BepInEx\LogOutput.log`（**建议临时把 `界面诊断日志` 打开**）
3. 游戏版本 + 补丁版本 + 读屏软件版本
4. 如果能复现，写清「按了什么键 → 期望什么 → 实际什么」

> 贴日志时留意一下：`[朗读]` 行就是台词原文。如果你要把日志公开贴出来
> （比如发到 Issue 里），**记得先把自己不想公开的那几行删掉**。
